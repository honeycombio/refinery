services:
  refinery:
    extends:
      file: common-services.yaml
      service: refinery

  redis:
    volumes:
      - redis-data:/data
    extends:
      file: common-services.yaml
      service: redis

volumes:
  redis-data:
